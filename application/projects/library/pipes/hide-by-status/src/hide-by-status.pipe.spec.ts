import { HideByStatusPipe } from './hide-by-status.pipe';

describe('HideByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new HideByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
