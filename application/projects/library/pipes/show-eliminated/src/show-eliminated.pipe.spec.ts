import { ShowEliminatedPipe } from './show-eliminated.pipe';

describe('ShowEliminatedPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowEliminatedPipe();
    expect(pipe).toBeTruthy();
  });
});
