mutation AddRating(
  $application_id: uuid!
  $round_id: Int!
  $value: Int!
  $reason: String!
) {
  insert_ratings_one(
    object: {
      application_id: $application_id
      round_id: $round_id
      value: $value
      message: { data: { text: $reason, application_id: $application_id } }
    }
  ) {
    id
    application {
      rated_by_user
    }
  }
}

# {
#   "round_id": 9,
#   "application_id": "3b7ce1e8-3095-4d41-803f-2b7c227728a4",
#   "reason": "too bad",
#   "value": 2
# }
