import { UploadTaskDirective } from './upload-task.directive';

describe('UploadTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadTaskDirective();
    expect(directive).toBeTruthy();
  });
});
