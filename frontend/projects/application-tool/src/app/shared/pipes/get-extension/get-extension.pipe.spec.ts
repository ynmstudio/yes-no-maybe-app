import { GetExtensionPipe } from './get-extension.pipe';

describe('GetExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
